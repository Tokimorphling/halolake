OPENAI_API_KEY=your-openai-compatible-upstream-key

# Optional. Required only when using examples/gateway.toml with the
# anthropic-main channel enabled.
ANTHROPIC_API_KEY=your-anthropic-compatible-upstream-key
